[pytest]
pythonpath = .
testpaths = tests
# Third-party deprecations inside Starlette's test client, not ours.
filterwarnings =
    ignore::DeprecationWarning:starlette.*
    ignore:Using `httpx` with `starlette.testclient`
