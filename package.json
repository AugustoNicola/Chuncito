{
  "name": "chuncito",
  "private": true,
  "description": "Heroku's build entry point. The app is in frontend/ and backend/; this only builds the frontend for the backend to serve.",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "heroku-postbuild": "npm --prefix frontend ci --include=dev && npm --prefix frontend run build && rm -rf frontend/node_modules"
  }
}
